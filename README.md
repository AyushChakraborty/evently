### Evently

### A University Event booking and management web application. This is the final project of the DBMS course.

### the mysql db is hosted on railways for easy access

### For now, to run the backend locally, get the .env file (secrets), then run

```bash
uvicorn backend.main:app --reload
```

### run this from the root
